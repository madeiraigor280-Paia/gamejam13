var _no_chao = place_meeting(x, y + 1, obj_bloco)





if (ativado)
{
	velv = 2
	grav = 0.01
	//Coloquei aqui para contar a gravidade mesmo sem o modo
	if (!_no_chao)
	{
	
		velv += gravidade	
	
	}
	
}